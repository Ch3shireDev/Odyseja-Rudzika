export enum Sex{
  Male,
  Female
}

export enum Direction {
  Spain,
  Italy,
  Greece,
}

export enum WindDirection {
  BeakWind,
  SideWind,
  TailWind,
}

export enum WindType {
  None,
  WeakWind,
  MediumWind,
  StrongWind,
  VeryStrongWind,
  Hurricane,
}

export enum FeedingGround {
  VeryWeak,
  Weak,
  Medium,
  Good,
}

export enum Health {
  Healthy,
  Injured,
  Dead,
}

export enum Rainfall {
  None,
  Sprinkle,
  LightRain,
  ModerateRain,
  HeavyRain,
  ViolentRain,
}

export enum WingType {
  StronglyPointed,
  SlightyPointed,
  Neutral,
  SlightyRounded,
  StronglyRounded,
}

export enum DecisionEnum {
  IntensiveFeeding,
  FitFeeding,
  Fly,
  SwitchFeeding,
  Recover
}

